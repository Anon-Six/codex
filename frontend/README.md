# INTEGRA Frontend

This directory contains the React interface for INTEGRA V6. The main component is `IntegraDemo.jsx` which renders the full application UI with chat, controls and hologram effects.

Open `index.html` in a browser (or via the Electron wrapper) to load the interface. The page uses React and Babel from CDN so no build step is required.
